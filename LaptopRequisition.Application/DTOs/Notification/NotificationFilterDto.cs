namespace LaptopRequisition.Application.DTOs.Notification
{
    public class NotificationFilterDto
    {
        public bool UnreadOnly { get; set; } = false;
        // Potentially add pagination properties later if needed
    }
}